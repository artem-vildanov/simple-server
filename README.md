Its a simple server, that can serve several parallel connections. Uses non-blocking sockets. So far only recieves client message and sends response. Planning to implement some http support in future.
